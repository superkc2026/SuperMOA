# SuperMOA 隐私声明

**最后更新：2026-07-29**

## 概述

SuperMOA 是一款**本地运行**的软件，所有数据处理和存储均在用户本地完成。我们高度重视您的隐私，以下声明详细说明了数据的处理方式。

## 1. 数据存储位置

- 所有配置、日志、用量数据均存储在用户本地目录：`~/.moa-gateway/`
- 不依赖任何云端服务器进行数据存储
- 用户可随时通过文件管理器访问、查看或删除该目录下的所有文件

## 2. 数据收集

**SuperMOA 不收集、不上传、不外传任何用户数据。**

具体而言：

- **不收集**：不会主动采集任何用户信息（不包含埋点、遥测、用户行为追踪）
- **不上传**：不会将任何数据发送到第三方服务器
- **不外传**：不会通过任何方式将用户数据传输给外部系统

## 3. API Key 加密存储

- API Key 使用 Windows DPAPI（Data Protection API）进行加密存储
- 加密后的密钥文件位于 `~/.moa-gateway/` 目录下
- 加密绑定当前用户账户，其他用户无法解密
- macOS/Linux 环境使用 `cryptography.fernet` 作为替代加密方案
- API Key 绝不以明文形式持久化存储

## 4. 调用日志

- 调用日志（请求/响应记录）仅存储在**本地**文件中
- 日志文件位于 `~/.moa-gateway/` 目录下
- 日志支持自动轮转（超过阈值自动清理旧记录）
- 用户可随时通过管理界面或手动删除日志文件
- 日志不包含完整的请求体内容（仅记录元信息：时间、模型、token 数）

## 5. 用量统计

- 用量统计数据（token 消耗、成本估算）存储在**本地 SQLite 数据库**中
- 数据库文件：`~/.moa-gateway/usage.db`
- 统计数据**不联网上报**，不发送到任何远程服务器
- 数据仅用于本地展示（用量看板），帮助用户了解自身使用情况
- 用户可随时清除用量统计数据库

## 6. 错误上报

- 错误上报功能**默认关闭**（opt-in，需用户手动开启）
- 开启方式：在配置页勾选「错误上报」选项
- 开启后：未捕获的异常将写入本地文件 `~/.moa-gateway/errors.jsonl`
- 上报内容为**脱敏堆栈信息**：
  - 包含：异常类型、错误消息（脱敏后）、调用栈（文件名+行号+函数名）
  - **不包含**：HTTP 请求体内容、API Key、用户输入文本、响应内容
- 错误上报**仅写入本地文件，不联网发送**
- 用户可随时关闭该功能或删除错误日志文件

## 7. 版本更新检查

- 版本更新检查功能通过访问腾讯云对象存储（COS）上的 `versions.json` 获取最新版本信息
- 仅发送 HTTP GET 请求到腾讯云 COS 地址，请求中不包含任何用户数据
- 不自动下载更新文件，仅提示用户有新版本可用
- 用户可手动触发或关闭版本检查功能

## 8. 网络通信

SuperMOA 的网络通信仅限于：

| 通信对象 | 用途 | 携带用户数据 |
|----------|------|-------------|
| AI 模型 API | 转发用户对话请求 | 是（用户主动发起的请求） |
| 腾讯云 COS | 版本更新检查 | 否（仅 GET 请求） |

- 与 AI 模型的通信是用户主动发起的对话请求，SuperMOA 仅作为代理转发
- SuperMOA 本身不运营任何中间服务器，不截留用户对话内容

## 9. 用户权利

用户拥有以下权利：

- **访问权**：可随时查看 `~/.moa-gateway/` 目录下的所有文件
- **删除权**：可随时删除任意或全部本地数据文件
- **知情权**：本声明完整披露了所有数据处理方式
- **选择权**：可自主决定是否开启错误上报、版本检查等功能
- **退出权**：可随时关闭已开启的任何功能（关闭后立即停止相关数据记录）

## 10. 联系方式

如有隐私相关问题，请通过 GitHub Issues 提交。

---

**声明**：本隐私声明可能随版本更新而调整，以本地最新版本为准。
